sudo service cloudera-scm-server stop
sudo service cloudera-scm-server-db stop


for u in hdfs mapred cloudera-scm hbase hue zookeeper oozie hive impala flume; do sudo kill $(ps -u $u -o pid=); done

# sudo apt-get remove -y --purge $(dpkg -l | grep -ehadoop -ecloudera -ehue -eoozie -ehbase -eimpala -eflume -ehive | awk '{ print $2 }' )

# -qqy --force-yes 
sudo apt-get purge cloudera-manager-server cloudera-manager-server-db mahout whirr hue pig sqoop  flume oozie-client oozie hive hive-metastore hive-server hive-server2 hadoop-hbase hadoop-zookeeper-server hadoop-zookeeper hadoop-httpfs cdh3-repository hadoop-0.20

sudo apt-get clean

sudo rm -Rf /usr/share/cmf /var/lib/cloudera* /var/cache/yum/cloudera*

sudo rm /tmp/.scm_prepare_node.lock